export interface ApiResponse {
    success: boolean;
    adminToken: string;
    message: string;
    token : string
  }

export interface userData {
  userName: string,
  email: string,
  number: number,
  password: string,
}
  